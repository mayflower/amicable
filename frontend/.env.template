VITE_BEAM_WS_URL=your-agent-ws-url
# Token is only required if the agent enforces legacy ?auth_token=... auth.
# If the agent is configured for Google OAuth (AUTH_MODE=google), this can be left blank.
VITE_BEAM_TOKEN=

# Optional: explicit HTTP base URL for the agent (used for /auth/*).
# If omitted, it is derived from VITE_BEAM_WS_URL.
# VITE_AGENT_HTTP_URL=https://your-agent-host/
