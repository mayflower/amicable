"""Platform scaffolding for newly created Amicable projects.

This module is responsible for writing Backstage/Sonar/TechDocs starter files
into the sandbox workspace. It is intentionally best-effort and non-destructive:
it never overwrites existing files.
"""

