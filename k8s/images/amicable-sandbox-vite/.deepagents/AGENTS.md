# Project Memory (Sandbox)

This file exists as a default writable memory location for agents running inside the sandbox.

- Keep notes short and specific.
- Do not store secrets (API keys, tokens, passwords).

